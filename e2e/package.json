{
  "name": "notes-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright end-to-end + smoke tests for the Collaborative Notes App (runs against the full running stack)",
  "scripts": {
    "test": "playwright test",
    "test:smoke": "playwright test --grep @smoke",
    "install:browsers": "playwright install --with-deps chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
